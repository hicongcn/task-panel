# 后端
server/taskpanel-server
server/data/
server/*.log

# Docker 数据卷(docker-compose 挂载 ./data)
/data/

# 前端
web/node_modules/
web/dist/

# 通用
.DS_Store
*.log

# 本地数据备份(不进 git)
server/data_backup_*/

# 多平台发布二进制(可用 releases/build-all.sh 重新生成)
/releases/taskpanel-server-*

# 内嵌前端构建产物(由 releases/build-all.sh 同步,保留占位文件)
/server/webembed/dist/*
!/server/webembed/dist/PLACEHOLDER.txt
